function traverseTree(json)
{
    json.targets[index].blocks;
    let blocks
    if (opcode === NULL) { return 0; }
    if (blocks === NULL) { return 0; }

    traverseTree(node.child);
}

function CountBlockPerType(opcode, inputBlockType, jsoninput)
{
    const code = opcode.split("_");
    code[0];

    if (code[0] = inputBlockType) {
        
    } 
}

object.targets.blocks